engine-strict=true

# npm 12+ supply-chain guard (allow-git defaults to "none"). This project
# consumes the private falcra stack as git dependencies pinned to semver tags —
# both direct (@falcra/homestead-game) and transitive (falcra, sools,
# homestead-assets), hence "all" rather than "root".
allow-git=all
